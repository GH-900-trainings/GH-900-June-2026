# Azure Maps authentication
# Get a key from the Azure Portal: Azure Maps account > Authentication > Shared Key
AZURE_MAPS_KEY=your-azure-maps-subscription-key-here

# Optional: port the server listens on (defaults to 3000)
PORT=3000
